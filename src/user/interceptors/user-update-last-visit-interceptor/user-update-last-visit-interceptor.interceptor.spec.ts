import { UserUpdateLastVisitInterceptorInterceptor } from './user-update-last-visit-interceptor.interceptor';

describe('UserUpdateLastVisitInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new UserUpdateLastVisitInterceptorInterceptor()).toBeDefined();
  });
});
